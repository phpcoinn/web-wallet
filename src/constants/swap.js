/** Testnet (CHAIN_ID 01) swap-to-mainnet bridge destination (legacy wallet / node). */
export const SWAP_BRIDGE_ADDRESS = 'PqfUcPFAVXFSnFtc3MzmXAwjgK42ACweur'

/** Minimum swap amount (PHP), matches legacy wallet. */
export const SWAP_MIN_AMOUNT = 1000

/** Swap UI and routing; set true when direct swap rollout completes (mirror legacy wallet swap flag when re-enabling). */
export const SWAP_ENABLED = false
